import CountUp from "react-countup";import PropTypes from "prop-types";import useIntersectionObserver from "@hooks/useIntersectionObserver";export default function AnimatedCounter({end,suffix,prefix}){const {ref,isVisible}=useIntersectionObserver();return <span ref={ref}>{isVisible?<CountUp end={end} suffix={suffix} prefix={prefix}/>:"0"}</span>;}AnimatedCounter.propTypes={end:PropTypes.number.isRequired,suffix:PropTypes.string,prefix:PropTypes.string};AnimatedCounter.defaultProps={suffix:"",prefix:""};